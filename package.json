{
  "name": "folio",
  "private": true,
  "version": "0.1.0",
  "description": "Architectural Intent Specification — tooling monorepo",
  "author": "Ivanovic Nosel",
  "license": "CC-BY-4.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.12.12"
  }
}
